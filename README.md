# Android-CallLogSeeder
This application is intended only for QA testing on physical Android devices.  The application must insert fake records into the Android Call Log without requiring root access.  The user must be able to define the exact number of records to create for each call type.
